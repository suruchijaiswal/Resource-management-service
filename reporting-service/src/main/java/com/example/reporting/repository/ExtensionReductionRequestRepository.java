/*
package com.example.reporting.repository;

import com.example.reporting.model.ExtensionReductionRequest;
import com.example.reporting.model.RequestStatus;
import org.springframework.data.jpa.repository.JpaRepository;
import java.util.List;

public interface ExtensionReductionRequestRepository extends JpaRepository<ExtensionReductionRequest, Long> {
    List<ExtensionReductionRequest> findByStatus(RequestStatus status);
    List<ExtensionReductionRequest> findByAllocationId(Long allocationId);

}*/
